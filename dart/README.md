# Dart basics

Language examples and exercises from the mobile development course, written in
plain Dart (no Flutter).

- `examples/` — one file per topic: variables, strings, math, conditions,
  repetition, functions and maps.
- `exercises/` — exercise lists, one folder per list.

## How to run

```bash
dart run examples/variables.dart
```
